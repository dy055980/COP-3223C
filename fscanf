# include <stdio.h>
int main(void) {
    int age;
    char initial;

    fscanf(stdin, "%d", &age);      // trailing space eats whitespace/lines
    fscanf(stdin, "%c", &initial);  // now reads the real character

    printf("Age: %d, Initial: %c\n", age, initial);
    return 0;

}